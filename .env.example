DB_HOST=
DB_NAME=
DB_USER=
DB_PASS=
APP_ENV=local
# Set to true temporarily to show PHP errors in the browser when debugging a 500.
APP_DEBUG=false
# If the host returns 500 on HTML routes only, try true temporarily to rule out CSP/WAF issues.
DISABLE_CSP=false
